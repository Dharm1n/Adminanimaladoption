<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tempanimaladoption" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TempAnimalAdoption" >
        <activity
            android:name=".ui.useractivity"
            android:label="@string/title_activity_useractivity"
            android:theme="@style/Theme.TempAnimalAdoption.NoActionBar" >
        </activity>
        <activity
            android:name=".user"
            android:label="@string/title_activity_user"
            android:theme="@style/Theme.TempAnimalAdoption.NoActionBar" >
        </activity>
        <activity
            android:name=".admin"
            android:label="@string/title_activity_admin"
            android:theme="@style/Theme.TempAnimalAdoption.NoActionBar" />
        <activity android:name=".manageotp" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>